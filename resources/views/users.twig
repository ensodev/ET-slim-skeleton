{% extends 'layouts/app.twig' %}

{% block title %} User {% endblock %}

{% block content %}
    <h1>User</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
        {% endfor %}

        {{ base_url() }}/user
    </ul>
   
{% endblock %}